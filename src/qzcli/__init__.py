"""qzcli — agent-first CLI for the 启智 platform (qz.sii.edu.cn).

Built entirely on the reverse-engineered web API (``/api/v1/*``, cookie auth).
No OpenAPI. Default output is JSON; ``--table`` is the only human-facing mode.
"""

__version__ = "0.1.0"
